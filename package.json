{
  "name": "realtime-list",
  "version": "1.0.0",
  "description": "Mini-app temps réel locale (WebSocket + SQLite)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "init:db": "node -e \"require('fs').readFileSync('schema.sql','utf8').split(/;\\s*\\n/).filter(Boolean).forEach(s=>require('better-sqlite3')('data.db').prepare(s).run())\""
  },
  "dependencies": {
    "better-sqlite3": "^9.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
